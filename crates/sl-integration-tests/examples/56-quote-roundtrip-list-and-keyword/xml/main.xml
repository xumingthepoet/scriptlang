<module name="main">
  <!-- Step 2.5: quote/unquote with list and keyword opts.
       A list opts can be unquoted inside <quote> to generate multiple text nodes.
       A keyword opts can be bound and its values used for conditional logic. -->
  <use module="provider" items="foo,bar,baz" enabled="true"/>
</module>
