# Design Principles

- Keep XML authoring surface small and legible.
- Favor explicit semantics over magic behavior.
- Make implicit runtime groups observable through diagnostics.
- Keep language primitives minimal when TypeScript code nodes can express logic directly.

